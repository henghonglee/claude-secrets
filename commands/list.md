---
description: List all available secrets in the vault
---

Call the `list_secrets` MCP tool to show the user all available secrets with their names, descriptions, and expiration times. Format the output as a clear table or list.
